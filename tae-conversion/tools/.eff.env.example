# Copy to .eff.env (same folder) and fill in. effbug.py reads this automatically.
# Keep .eff.env chmod 600 and never commit it (.gitignore already ignores it).
BUGZILLA_API_KEY=your-bmo-api-key-here
